//Take four parameters. Multiply the four numbers and then return the result

function multiplyFunc(n1, n2, n3, n4) {
  const n = n1 * n2 * n3 * n4;
  return n;
}

const result = multiplyFunc(2, 3, 4, 5);
console.log(result);
